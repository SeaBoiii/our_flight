# The origin only (no trailing path) of the private Sites API deployment.
VITE_API_ORIGIN=https://your-private-api.example

# Optional for a custom domain. GitHub-hosted builds derive the Pages URL automatically.
VITE_PUBLIC_SITE_URL=https://your-public-site.example/
